export function createActionsMap(actions: any): Map<any, any>;
export namespace FieldType {
    let none: number;
    let number: number;
    let percent: number;
    let date: number;
    let time: number;
}
export function getFieldType(actions: any): number;
